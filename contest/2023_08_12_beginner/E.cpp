#include <bits/stdc++.h>
using namespace std;

typedef pair<double, double> pdd;

int main()
{
    int n, m;
    cin >> n >> m;

    vector<pdd> route;

    for (int i = 0; i < n; i++) {
        int c, p;
        cin >> c >> p;

        int sum = 0;
        for (int j = 0: j < p; j++) {
            int t;
            cin >> t;
            sum += t;
        }

        route.emplace_back()
    }

}