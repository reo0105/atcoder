#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main()
{
    int n;
    vector<vector<int>> c(70000, vector<int>(20));

    cin >> n;
    for (int i = 1; i <= (1<<n); i++) {
        for (int j = 1; j <= n; j++) {
            cin >> a.at(i).at(j);
        }
    }


}