#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    vector<int> P(200005), I(200005);

    cin >> n;
    for (int i = 0; i < n; i++) cin >> P.at(i);
    for (int i = 0; i < n; i++) cin >> I.at(i);

    
}