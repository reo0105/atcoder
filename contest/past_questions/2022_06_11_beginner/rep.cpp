#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main()
{
    int n, m;
    vector<int> s(100005);
    vector<int> x(15);

    cin >> n >> m;
    for (int i = 1; i <= n-1; i++) cin >> s.at(i);
    for (int i = 1; i <= m; i++) cin >> x.at(i);

    

    return 0;
}