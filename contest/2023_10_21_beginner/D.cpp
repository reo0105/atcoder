#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main()
{
    int n;
    cin >> n;

    vector<ll> td(n);
    for (int i = 0; i < n; i++) cin >> td[i].first >> td[i].second;

    sort(td.begin(), td.end());


    ll now = 1;
    int id = 0;
    for (ll i = 1; i < 1e18; i++) {
        
        while (td[id].first <=)


    }

    return 0;
}